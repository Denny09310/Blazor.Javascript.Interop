@namespace SampleWebApp.Components

<i class="fa fa-flask @IconSize text-info" title="This is an ExperimentalIcon feature" />

@code
{
    [Parameter]
    public string? Size { get; set; }

    protected string IconSize => $"fa-{Size ?? "xs"}";
}